<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xieminjie.mapper.ProductMapper">

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO product (
        serial_number, product_name, category, model, batch_number,
        manufacturer, created_time, updated_time
        ) VALUES
        <foreach collection="products" item="item" separator=",">
            (
            #{item.serialNumber}, #{item.productName}, #{item.category},
            #{item.model}, #{item.batchNumber}, #{item.manufacturer},
            #{item.createdTime}, #{item.updatedTime}
            )
        </foreach>
    </insert>

    <select id="selectBySerialNumbers" resultType="com.xieminjie.entity.Product">
        SELECT * FROM product
        WHERE serial_number IN
        <foreach collection="serialNumbers" item="sn" open="(" separator="," close=")">
            #{sn}
        </foreach>
    </select>

    <select id="selectById" resultType="com.xieminjie.entity.Product">
        SELECT * FROM product WHERE id = #{id}
    </select>

</mapper>